import { ValueRange } from './value-range';

describe('ValueRange', () => {
  let valueRange: ValueRange;

  describe('root', () => {
    it('should work', () => {
      expect(true).toBe(true);
    });
  });
});
